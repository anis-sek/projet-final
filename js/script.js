
$(document).ready(function () {
    // Initialisation de Lightcase avec configuration
    $('a[data-rel^=lightcase]').lightcase({
        closeOnOverlayClick: true, // Fermer en cliquant sur l'overlay (activé par défaut)
    });
});
